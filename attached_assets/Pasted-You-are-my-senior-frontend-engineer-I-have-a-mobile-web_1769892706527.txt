You are my senior frontend engineer. I have a mobile web React app with an interactive map underneath (Google Maps / Mapbox style). I already render a list overlay at the bottom, but it’s clunky: touch events sometimes pan the map, the sheet won’t reliably drag down to close, and scrolling inside the sheet fights dragging.

Build a PRODUCTION-QUALITY bottom sheet overlay that behaves like Resy/Airbnb:

Goal behavior:
- The sheet is overlayed on top of the map and is always “touch dominant” when interacting with the sheet.
- Three snap points:
  1) COLLAPSED: grabber only (~56px tall)
  2) MID: ~30% of viewport height (default on open)
  3) EXPANDED: ~90% of viewport height
- Dragging up/down (touch + mouse) smoothly moves the sheet and snaps to the nearest snap point on release (consider velocity).
- When the sheet is expanded, the sheet content scrolls internally.
- IMPORTANT: Drag vs scroll handoff:
  - If the user scrolls the content normally, it should scroll.
  - If the content scroll is at the TOP and the user drags DOWN, it should drag the sheet down (collapse).
  - If the user drags UP from the grabber area, it expands.
- Prevent the underlying map from receiving pointer/touch events when the user interacts with the sheet (including the content area). The map must not pan/zoom due to sheet gestures.
- Lock body scroll while sheet is mid or expanded. Avoid iOS Safari rubber-banding.
- Handle safe area (iPhone notch): respect env(safe-area-inset-bottom).
- Add a semi-transparent backdrop ONLY when expanded (tapping backdrop snaps to MID).
- Performance: use transform: translate3d, requestAnimationFrame where needed; no jank.

Implementation constraints:
- Use React + TypeScript.
- Use framer-motion for drag + snap if possible (install if needed).
- No heavy UI frameworks.
- Output these files:
  - components/BottomSheet.tsx
  - hooks/useBodyScrollLock.ts
  - hooks/useElementScrollTop.ts (or similar helper to detect content scrollTop)
  - pages/MapWithSheet.tsx (example usage)
  - minimal CSS (CSS module or plain CSS)

Example page behavior:
- Map is a full-screen div placeholder (or my existing map container).
- Bottom sheet contains:
  - a grabber
  - a header row with a “Search here” button
  - a scrollable list of cards (mock items ok)
- Make sure pointer events are correct:
  - The sheet captures interactions
  - The map remains interactive when touching the map area (outside sheet), but not when touching/dragging on the sheet.

Return complete code with comments explaining the key parts (drag vs scroll handoff and preventing map interactions).
